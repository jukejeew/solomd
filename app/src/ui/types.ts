export interface DsSelectOption {
  value: string;
  label: string;
  disabled?: boolean;
}

export interface DsDropdownItem {
  value: string;
  label: string;
  disabled?: boolean;
}

export interface DsTab {
  value: string;
  label: string;
  disabled?: boolean;
}
